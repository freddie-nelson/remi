#pragma once

namespace Rendering
{
    /**
     * Represents the active camera.
     *
     * The active camera is the camera that is used to render the scene on update.
     */
    struct ActiveCamera
    {
    };
}